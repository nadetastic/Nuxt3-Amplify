{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt -p 5173 dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview -p 5173",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.1"
  },
  "dependencies": {
    "@aws-amplify/ui-vue": "^3.1.6",
    "aws-amplify": "^5.0.12"
  }
}
